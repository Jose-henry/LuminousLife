import styles from "./pageText2.module.css"; // Import CSS module

const PageText2: React.FC = () => {
    return (
        <h2 className={styles.text}>Power Up with Solar!!!</h2>
    );
}

export default PageText2;
